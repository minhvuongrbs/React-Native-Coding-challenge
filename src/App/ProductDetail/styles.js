import { StyleSheet } from 'react-native'
export default styles = StyleSheet.create({
    mainSection: {
        flex: 1,
        flexDirection: 'column',
    },
    cartSection: {
        flexDirection: 'row',
        alignItems: 'center',
        elevation: 10,
        marginBottom: 10
    }
});